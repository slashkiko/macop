name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality:
    runs-on: macos-latest
    steps:
      - name: Checkout
        # v4.2.2
        uses: actions/checkout@d4537ff835c9778c934e48f78639e270edd5839e

      - name: Setup Swift
        # v2.2.0
        uses: swift-actions/setup-swift@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          swift-version: "6.2"

      - name: Install lint and format tools
        run: brew install swiftformat swiftlint

      - name: Format check
        run: make format-check

      - name: Lint
        run: make lint

      - name: Build
        run: make build

      - name: Test
        run: make test
